# Applications

Screens only. Expand this folder for every **product surface a person uses**.

Import `site-appearance/` for look and `core/` for behavior. Never import another application.

Staff / moderation is not an application. That work lives in `core/moderation/` when it exists.

Retired screens: `archive/` — live code never imports it.
