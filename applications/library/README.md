# Library

Public browse shelf at `/library`. Uses `core/library`, `core/publishing`, and `site-appearance/`.

Authors post from Studio gear → `/publish?book=`. This folder is the reader-facing catalog only.
