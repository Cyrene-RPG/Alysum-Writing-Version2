# Encyclopedia

Later rebuild: World Encyclopedia UI (lore shelf). Geography, magic, city, and realm builders are **pages inside this app**, not sibling apps.

Uses `core/encyclopedia` and `design-system/`. Do not put live pages here yet.
