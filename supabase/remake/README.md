# Remake migrations

SQL written for this remake. Do not run on the live project.

- `migrations/supabase-books.sql` — uuid ids, `created_at` / `updated_at`
- `migrations/supabase-author-bio.sql` — `users.bio`
