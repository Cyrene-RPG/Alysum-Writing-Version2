    .wd-main {
        flex: 1;
        max-width: 1280px;
        width: 100%;
        margin: 0 auto;
        padding: 20px clamp(16px, 3vw, 32px) 48px;
    }

    .wd-stats-block {
        border-radius: 18px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.01));
        overflow: hidden;
    }

    .wd-stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 0;
        margin: 0;
        padding: 0;
        list-style: none;
    }

    .wd-stat {
        margin: 0;
        padding: 20px 12px;
        min-height: 92px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        border-right: 1px solid rgba(255, 255, 255, 0.06);
    }

    .wd-stat:last-child { border-right: none; }

    .wd-stat strong {
        display: block;
        font-size: clamp(22px, 4vw, 26px);
        font-weight: 900;
        line-height: 1.1;
        color: #fbbf24;
    }

    .wd-stat-label {
        display: block;
        margin-top: 8px;
        font-size: 12px;
        font-weight: 600;
        color: #6b7280;
        letter-spacing: 0.02em;
    }

    .wd-stats-foot {
        display: grid;
        grid-template-columns: auto 1fr;
        align-items: center;
        gap: 10px 14px;
        padding: 12px 16px;
        border-top: 1px solid rgba(255, 255, 255, 0.08);
        font-size: 12px;
        font-weight: 600;
        color: #6b7280;
    }

    .wd-stats-foot-label {
        border: none;
        background: transparent;
        color: inherit;
        padding: 0;
        font: inherit;
        cursor: pointer;
        white-space: nowrap;
        text-align: left;
    }

    .wd-stats-foot-label:hover {
        color: #cbd5e1;
    }

    .wd-stats-foot-count,
    .wd-goal-edit-btn {
        justify-self: end;
        color: #d1d5db;
        font-weight: 700;
        white-space: nowrap;
    }

    .wd-goal-edit-btn {
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.04);
        padding: 7px 11px;
        font: inherit;
        cursor: pointer;
        transition: background 0.12s ease, border-color 0.12s ease, color 0.12s ease;
    }

    .wd-goal-edit-btn:hover,
    .wd-goal-edit-btn[aria-expanded="true"] {
        color: #fff;
        border-color: rgba(167, 139, 250, 0.38);
        background: rgba(124, 58, 237, 0.18);
    }

    .wd-goal-picker {
        grid-column: 1 / -1;
        display: none;
        padding: 14px;
        border: 1px solid rgba(167, 139, 250, 0.2);
        border-radius: 16px;
        background: rgba(2, 6, 23, 0.62);
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
    }

    .wd-goal-picker.is-open { display: block; }

    .wd-goal-picker-title {
        margin: 0;
        color: #f8fafc;
        font-size: 13px;
        font-weight: 900;
    }

    .wd-goal-picker-hint {
        margin: 4px 0 12px;
        color: #94a3b8;
        font-size: 12px;
        line-height: 1.4;
    }

    .wd-goal-presets {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .wd-goal-preset {
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.05);
        color: #e5e7eb;
        padding: 8px 11px;
        font: inherit;
        font-size: 12px;
        font-weight: 800;
        cursor: pointer;
    }

    .wd-goal-preset:hover,
    .wd-goal-preset.is-active {
        border-color: rgba(251, 191, 36, 0.42);
        background: rgba(251, 191, 36, 0.14);
        color: #fef3c7;
    }

    .wd-goal-custom {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        align-items: center;
        margin-top: 12px;
    }

    .wd-goal-custom input {
        flex: 1 1 160px;
        min-width: 0;
        height: 38px;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 12px;
        background: rgba(15, 23, 42, 0.92);
        color: #f8fafc;
        padding: 0 11px;
        font: inherit;
        font-weight: 800;
        outline: none;
    }

    .wd-goal-custom input:focus {
        border-color: rgba(167, 139, 250, 0.55);
        box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.2);
    }

    .wd-goal-save,
    .wd-goal-cancel {
        height: 38px;
        border-radius: 12px;
        border: 1px solid rgba(255, 255, 255, 0.12);
        padding: 0 12px;
        font: inherit;
        font-size: 12px;
        font-weight: 900;
        cursor: pointer;
    }

    .wd-goal-save {
        border-color: transparent;
        background: #7c3aed;
        color: #fff;
    }

    .wd-goal-cancel {
        background: transparent;
        color: #cbd5e1;
    }

    .wd-goal-picker-status {
        min-height: 1.2em;
        margin-top: 10px;
        color: #94a3b8;
        font-size: 12px;
        font-weight: 700;
    }

    .wd-goal-bar {
        grid-column: 1 / -1;
        height: 6px;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
        overflow: hidden;
    }

    .wd-goal-fill {
        height: 100%;
        width: 0%;
        border-radius: 999px;
        background: #a78bfa;
        transition: width 0.3s ease;
    }

    .wd-goal-fill.is-met { background: #4ade80; }

